# Termatica 1.3.1

## Fix

- Fixed `termatica bench` helper install failing on first run with "The folder doesn't exist". The `~/Library/Application Support/Termatica/` directory is now created before writing the helper script.

## Scope

- No other changes from 1.3.0. Bundle under 1.03 MiB.